{
  "kind": "collectionType",
  "collectionName": "tournements",
  "info": {
    "name": "Tournament",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false,
    "privateAttributes": [
      "_id",
      "__v"
    ]
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string",
      "unique": true,
      "required": true
    },
    "userOwner": {
      "plugin": "users-permissions",
      "model": "user",
      "via": "tournament"
    },
    "boards": {
      "via": "tournament",
      "collection": "board"
    },
    "slug": {
      "type": "uid",
      "targetField": "name"
    },
    "matchKnockouts": {
      "via": "tournament",
      "collection": "match-knockout"
    }
  }
}
